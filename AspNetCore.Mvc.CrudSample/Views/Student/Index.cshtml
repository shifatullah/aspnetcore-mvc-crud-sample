@using AspNetCore.Mvc.CrudSample.Entities
@model StudentListViewModel

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <a class="btn btn-primary" role="button" asp-controller="Student" asp-action="New">New Student</a>
        </div>
    </div>

    <table class="table table-hover">
        <thead>
            <tr>
                <th style="width: 25%" scope="col">Id</th>
                <th style="width: 50%" scope="col">Name</th>
                <th style="width: 25%" scope="col">Actions</th>
            </tr>
        </thead>

        <tbody>
            @foreach (Student student in Model.Students)
            {
                <tr>
                    <td scope="row">@student.Id</td>
                    <td>@student.Name</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@student.Id">Edit</a> |
                        <a asp-action="Delete" asp-route-id="@student.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>